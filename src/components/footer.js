import { Typography } from "@mui/material";

const Footer = () => {
  return (
    <div
      style={{ display: "flex", flexDirection: "column", alignItems: "center" }}
    >
      <Typography color="primary"> [R I C K - E D]</Typography>
      <Typography color="text.primary"> All rights reserved</Typography>
    </div>
  );
};

export default Footer;
